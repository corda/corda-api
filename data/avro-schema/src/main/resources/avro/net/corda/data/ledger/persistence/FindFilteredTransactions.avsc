{
  "type": "record",
  "name": "FindFilteredTransactions",
  "doc": "Find filtered transactions with the given ids. One of several types of ledger persistence request {@link LedgerPersistenceRequest}",
  "namespace": "net.corda.data.ledger.persistence",
  "fields": [
    {
      "name": "ids",
      "type": {
        "type": "array",
        "items": "string"
      },
      "doc": "The ids of the filtered transactions to find."
    }
  ]
}
