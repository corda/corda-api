{
  "type": "record",
  "name": "ChunkAckKey",
  "namespace": "net.corda.data.chunking",
  "doc": "Key object for {@link net.corda.data.chunking.ChunkAck} when sent in repsonse to receipt of {@link net.corda.data.chunking.Chunk}",
  "fields": [
    {
      "name": "requestId",
      "type": "string",
      "doc": "unique identifier that indicates the group this chunk belongs to"
    },
    {
      "name": "partNumber",
      "type": "int",
      "doc": "number of chunk"
    }
  ]
}
