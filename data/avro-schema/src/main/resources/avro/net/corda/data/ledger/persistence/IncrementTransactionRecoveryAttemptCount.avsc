{
  "type": "record",
  "name": "IncrementTransactionRepairAttemptCount",
  "doc": "Increments the repair attempt count column. One of several types of ledger persistence request {@link LedgerPersistenceRequest}",
  "namespace": "net.corda.data.ledger.persistence",
  "fields": [
    {
      "name": "id",
      "type": "string",
      "doc": "The transaction ID, derived from the root hash of its Merkle tree"
    }
  ]
}
