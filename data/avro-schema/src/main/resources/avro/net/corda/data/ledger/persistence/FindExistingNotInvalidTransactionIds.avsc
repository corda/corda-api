{
  "type": "record",
  "name": "FindExistingNotInvalidTransactionIds",
  "doc": "A request to fetch transaction that are not invalid (i.e. Verified or Unverified).",
  "namespace": "net.corda.data.ledger.persistence",
  "fields": [{
    "name": "transactionIds",
    "type": {
      "type" : "array",
      "items" : "string"
    },
    "doc": "A list of the transaction IDs to fetch."
  }]
}
