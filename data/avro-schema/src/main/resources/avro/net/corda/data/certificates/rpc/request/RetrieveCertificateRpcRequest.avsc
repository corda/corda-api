{
  "type": "record",
  "name": "RetrieveCertificateRpcRequest",
  "namespace": "net.corda.data.certificates.rpc.request",
  "doc": "RPC request to retrieve a certificate chain.",
  "fields": [
    {
      "name": "alias",
      "doc": "The certificate alias. If null, all certificates (for specified tenant) will be retrieved",
      "type": [
        "string",
        "null"
      ]
    }
  ]
}