{
  "type": "record",
  "name": "QueryQueuedRegistrationRequest",
  "namespace": "net.corda.data.membership.db.request.query",
  "doc": "Query for the oldest registration request having `NEW` status",
  "fields": [
    {
      "name": "requestSubjectShortHash",
      "doc": "Holding identity short hash of the subject of the registration requests.",
      "type": "string"
    }
  ]
}