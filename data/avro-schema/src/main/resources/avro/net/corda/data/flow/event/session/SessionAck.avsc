{
  "type": "record",
  "name": "SessionAck",
  "doc" : "Acknowledge to another party that their session message was received.",
  "namespace": "net.corda.data.flow.event.session",
  "fields": [
    {
      "name": "isWaitingForData",
      "type": "boolean",
      "doc": "Flag is set to true is session is waiting to receive data."
    }
  ]
}
